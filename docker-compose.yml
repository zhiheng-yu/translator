services:
  api:
    build: .
    ports:
      - "6003:6003"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_BASE_URL=${OPENAI_BASE_URL}
    restart: unless-stopped
    env_file:
      - .env
